<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
                      http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- Pom parent -->
    <parent>
        <groupId>es.gva.ptt.clave</groupId>
        <artifactId>ptt-clave</artifactId>
        <version>1.0.0</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <!-- Artifact -->
    <groupId>es.gva.ptt.clave</groupId>
    <artifactId>ptt-clave-proc</artifactId>
    <version>${componente.version}</version>
    <name>Capa de procesos</name>
    <description>Capa de procesos</description>
    <packaging>jar</packaging>
    
    <properties>
        <componente.version>1.0.0</componente.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <java.version>1.6</java.version>
    </properties>

    <!-- Dependencias -->
    <dependencies>
        <!-- Dependencias internas (otros modulos) -->
        <dependency>
            <groupId>es.gva.ptt.clave</groupId>
            <artifactId>ptt-clave-neg</artifactId>
            <version>${componente.version}</version>
            <type>jar</type>
        </dependency>
    </dependencies>
</project>
